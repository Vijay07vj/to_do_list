import React from "react";

const Header = () => {
  return (
    <header>
      <h1>To Do App</h1>
    </header>
  );
};

// Header.defaultProps = {
//   title: "To Do App",
// };

export default Header;
